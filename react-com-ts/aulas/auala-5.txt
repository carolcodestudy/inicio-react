Aula 5

31/03/25

Arrays

Passa uma lista em useState.
map()-> Pecorre a lista
() retorna algo para interface
Passar a chave dentro da tag key{item_da_lista}

//Lista
const [task, setTask] = useState([
  "Orar",
  "Estudar inglês",
  "Caminhar",
  "Assistir vídeos da auto escola."
])

//Nova tarefa
const [ newTask, setNewTask ] = useState("")

//setTask([ newTask ]) Não adicona na lista, paenas coloca e as outrs tarefas não são exibidas

Para que o evento seja acionado no item especifico para apagar, tem que chamar uma função anõnima que receberá a função de deletar dentro de onClick no botão. onClick={ ()=> DelTask(iten) }